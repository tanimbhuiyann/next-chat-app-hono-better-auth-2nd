import Navbar from '@/components/verticalNavbar.tsx/Navbar';
import React from 'react';

const page = () => {
    return (
        <div>
            <Navbar/>
        </div>
    );
};

export default page;